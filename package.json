{
  "name": "sample-projects-on-typescript",
  "version": "1.0.0",
  "description": "ts",
  "main": "dist/src/index.js",
  "scripts": {
    "tree_build": "cd sysObjects && npm run tree_build && cd .. && npm install && npm run build",
    "start": "tsc -w",
    "build": "tsc",
    "test": "jest"
  },
  "repository": {
    "type": "git",
    "url": "ccc"
  },
  "author": "ed",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.3.4"
  },
  "dependencies": {
    "sysObjects": "file:sysObjects"
  }
}
