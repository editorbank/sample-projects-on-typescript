{
  "name": "sysObjects",
  "version": "1.0.0",
  "description": "ts",
  "main": "dist/src/index.js",
  "scripts": {
    "start": "tsc -w",
    "build": "tsc",
    "test": "jest",
    "prebuild": "git submodule foreach npm run build",
    "preinstall": "git submodule update --init --remote --recursive && git submodule foreach --recursive npm install"
  },
  "repository": {
    "type": "git",
    "url": "ccc"
  },
  "author": "ed",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.3.4"
  },
  "dependencies": {
    "sysGroup": "file:sysGroup"
  }
}
