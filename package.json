{
  "name": "sysObjects",
  "version": "1.0.0",
  "description": "ts",
  "main": "dist/src/index.js",
  "scripts": {
    "tree_up": "git submodule update --init --remote --recursive",
    "tree_build": "git submodule foreach npm run tree_build && npm install && npm run build",
    "start": "tsc -w",
    "build": "tsc",
    "test": "jest"
  },
  "repository": {
    "type": "git",
    "url": "ccc"
  },
  "author": "ed",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.3.4"
  },
  "dependencies": {
    "sysGroup": "file:sysGroup"
  }
}
