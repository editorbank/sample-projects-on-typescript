{
  "name": "sample-projects-on-typescript",
  "version": "1.0.0",
  "description": "ts",
  "main": "dist/src/index.js",
  "scripts": {
    "gsm_up": "git submodule update --init --remote --recursive",
    "prebuild": "git submodule foreach npm run build && npm install",
    "start": "tsc -w",
    "build": "tsc",
    "test": "jest"
  },
  "repository": {
    "type": "git",
    "url": "ccc"
  },
  "author": "ed",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.3.4"
  },
  "dependencies": {
    "sysObjects": "file:sysObjects"
  }
}
